version: 2

sources:
  - name: screen_time_base
    schema: base
    tables:
      - name: screen_time_event
        description: Sole analytical input; one resolved event per event_key, including inactive history.
